# Python
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Secrets — .env.example is committed, .env is NOT
.env
.env.local

# SQLite data — local persistence, not shared via git
backend/data/

# Node / Next.js
node_modules/
.next/
out/
npm-debug.log*

# Local run logs (e.g. uvicorn.log from `uvicorn ... > uvicorn.log`)
*.log

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
