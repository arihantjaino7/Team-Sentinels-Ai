# Sentinels backend environment
#
# Copy this file to `.env` in the same directory (backend/.env) and fill in
# real values there. `.env` is gitignored — this .env.example file is the one
# that's committed, and it must never contain a real key.

# --- GROQ_API_KEY (optional) ---------------------------------------------
#
# Powers the AI layer's plain-language summary (backend/ai/analyst.py) —
# 2-4 sentences at the top of every report, explaining the findings in
# plain English instead of just a score and a list.
#
# Get a free key at https://console.groq.com/keys (Groq's free tier is
# enough for this project — no paid plan needed).
#
# This is the ONLY thing this key affects. Sentinels is designed to work
# completely without it: leave this blank and every scan still runs all
# five agents and returns a full, correctly scored report — the `summary`
# field is just an empty string instead of AI-written text. Nothing errors,
# nothing degrades elsewhere, and no other feature (including PDF export)
# depends on this key in any way.
GROQ_API_KEY=

# --- SENTINELS_SESSION_SECRET (required for sign-in) --------------------
#
# Signs the session cookie (backend/auth/session.py). Any long random string
# works — e.g. `python -c "import secrets; print(secrets.token_urlsafe(32))"`.
# Unlike GROQ_API_KEY, this one is NOT optional: every route except /,
# /health, /agents, /repo/agents, and /auth/* requires a signed-in user, and
# without this secret there is no way to tell a real session from a forged
# one, so those routes refuse to serve at all (503) rather than silently
# letting everyone in.
SENTINELS_SESSION_SECRET=

# --- GITHUB_APP_CLIENT_ID / GITHUB_APP_CLIENT_SECRET (required) ---------
#
# From the "Sentinels Autofix" GitHub App's settings page
# (github.com/settings/apps -> your app -> General). These power
# "Sign in with GitHub" (backend/auth/github_oauth.py). Registering the App
# itself, and its repo-write permissions for later stages, is described in
# CONVENTIONS.md's remediation rules — this file only needs the two values
# below to make sign-in work.
GITHUB_APP_CLIENT_ID=
GITHUB_APP_CLIENT_SECRET=

# --- GITHUB_APP_SLUG / GITHUB_APP_ID / GITHUB_APP_PRIVATE_KEY_PATH -------
#
# Needed only for autofix (opening pull requests), not for sign-in.
#
# GITHUB_APP_SLUG is the App's URL name — the <slug> in
# github.com/apps/<slug>. It is NOT derivable from the display name, so it
# has to be set rather than guessed.
#
# GITHUB_APP_ID is the numeric "App ID" on the same settings page.
#
# GITHUB_APP_PRIVATE_KEY_PATH points at the .pem file generated from
# "Private keys" on that page. A *path*, deliberately, not the key itself:
# a multi-line PEM pasted into an environment variable ends up in shell
# history, CI logs, and screenshots. Keep the file OUTSIDE this repository.
#
# Without these three, sign-in still works and every scan still runs — only
# POST /scans/{id}/fix/apply refuses, with a message naming what's missing.
GITHUB_APP_SLUG=
GITHUB_APP_ID=
GITHUB_APP_PRIVATE_KEY_PATH=

# --- SENTINELS_ALLOW_DEV_TOKEN / SENTINELS_GITHUB_DEV_TOKEN (dev only) ---
#
# An escape hatch for testing the Git write path before the App is
# registered: a personal access token used in place of an installation
# token. BOTH must be set — the token alone does nothing unless
# SENTINELS_ALLOW_DEV_TOKEN is exactly "1". That is on purpose, so a token
# forgotten in a .env after a debugging session cannot stay live by
# accident. Only ever point this at a throwaway repository.
SENTINELS_ALLOW_DEV_TOKEN=
SENTINELS_GITHUB_DEV_TOKEN=

# --- SENTINELS_GITHUB_API_TOKEN (optional, recommended) ------------------
#
# Raises the rate limit on repo scans (POST /repo/scan, GET /repo/stream)
# from GitHub's unauthenticated 60 requests/hour-per-IP to 5,000/hour.
# Without it, a handful of repo scans in the same hour can lock out every
# visitor sharing this deploy's outbound IP until the limit resets.
#
# Any token works -- these are public-repo reads any token already gets, so
# no scopes are required. Easiest: github.com/settings/tokens -> "Generate
# new token (classic)" -> no scopes checked -> Generate. Used only for the
# two GitHub API calls in repo/fetch.py, never sent to any other host.
SENTINELS_GITHUB_API_TOKEN=

# --- SENTINELS_FRONTEND_ORIGIN (optional) --------------------------------
#
# Where the browser is sent after a successful sign-in. Defaults to
# http://localhost:3000, the Next.js dev server's default — only set this if
# your frontend runs somewhere else.
SENTINELS_FRONTEND_ORIGIN=
